import React from 'react';
import SearchForm from './SearchForm'

const Header = () => {
    return (
        <header>
            <h2>Note Manager</h2>
            <SearchForm />
        </header>
    );
}

export default Header;